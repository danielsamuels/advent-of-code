# AoC 2025

This year, I'm playing around with using `uv` as the "package manager" of sorts. My primary approach for running scripts is using VS Code's "run Python file" option.

I've not got a specific command for running the days, but presumably there's a `uv run ...` or something.